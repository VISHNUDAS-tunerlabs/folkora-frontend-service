/** Upper bound on travelers per booking — fills a 10x3 backpack grid. */
export const MAX_TRAVELERS = 30;

/** Number of columns in the backpack picker grid. */
export const BACKPACK_GRID_COLUMNS = 10;
